<div class="flex {{ $isColumn ? 'md:flex-col' : 'md:flex-row' }} flex-row items-center h-full w-max md:w-full md:gap-0 gap-3 justify-around">
    <img src="{{ $iconPath }}" />
    <span class="font-fredokaOne text-xl font-semibold">{{ $text }}</span>
</div>