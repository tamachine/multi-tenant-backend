<div class="max-w-2xl md:mx-auto">
    <h1 class="md-max:leading-9 text-center text-[40px] md:text-[70px]">{!! $title !!}</h1>         
    @if(isset($subtitle))
    <h4 class="mt-4 text-center font-sans text-base md:text-2xl leading-6 md:leading-8">{!! $subtitle !!}</h4>
    @endif
</div>